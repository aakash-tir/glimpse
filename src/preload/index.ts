// Preload runs in an isolated context with access to a curated subset of
// Node + Electron APIs. Bridges to be added as features land in later
// milestones. For M0 the preload is intentionally empty.
export {};
